# Skills – Contentstack Apps CLI

Canonical **index** (paths, titles, what each skill covers): **[`AGENTS.md`](../AGENTS.md)** → section **Where the documentation lives: skills**.

Each folder here is one skill; the file to open is always **`SKILL.md`**, with YAML frontmatter (`name`, `description`).
